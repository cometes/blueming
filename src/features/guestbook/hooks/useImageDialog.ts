/**
 * useCommentImageDialog의 alias.
 * 기존 import 경로(@/features/guestbook/hooks/useImageDialog) 호환 유지.
 */
export { useCommentImageDialog as useImageDialog } from "@/features/comment/hooks/useCommentImageDialog";
